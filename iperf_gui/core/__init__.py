"""Domain layer: configuration, process execution, parsing and sweeps.

Nothing in this package imports from :mod:`iperf_gui.ui`, so the whole domain
is testable without a running ``QApplication``.
"""
